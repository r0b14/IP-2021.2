#include "teste.h"

int isEven(int n) {

   return n % 2 == 0;
   /**
   int multiplicador = 1;
   for (int i = n; i > 0; i--);
   {
      multiplicador *= n;
      n--;
   }
   return multiplicador;
   //printf("%d\n", *n);
   **/
}